"use strict";
// alert(4+6);  we are using nodejs not browser
// window.alert("Hello world!");
// alert("Hello world!");
/*
Sure, here’s a brief summary of each JavaScript data type in one line:

### Primitive Data Types
1. *Number*: Represents numeric values, both integers and floating-point numbers (e.g., `42`, `3.14`).
2. **String**: Represents sequences of characters, enclosed in single, double quotes, or backticks (e.g., `'Hello'`, `"World"`).
3. **Boolean**: Represents logical values, either `true` or `false`.
4. **Undefined**: Represents an uninitialized variable or missing value (e.g., `let x;`).
5. **Null**: Represents an intentional absence of any object value (e.g., `let y = null;`).
6. **Symbol**: Represents a unique identifier, created using the `Symbol()` function (e.g., `let sym = Symbol('desc');`).
7. **BigInt**: Represents integers with arbitrary precision, denoted by appending `n` to a number (e.g., `12345678901234567890n`).

### Non-Primitive (Reference) Data Types
1. **Object**: Represents a collection of key-value pairs, including properties and methods (e.g., `let person = { name: 'John', age: 30 };`).
2. **Array**: Represents an ordered list of values, which can be of different types (e.g., `let arr = [1, 'two', true];`).
3. **Function**: A callable object that performs a task or calculates a value (e.g., `function greet() { console.log('Hello'); }`).
*/
let userName="ujjwal";
console.log(typeof  null);
// null is an object
// the type of undefined is undefined -
"use strict"; // treat all JS code as newer version

// alert( 3 + 3) // we are using nodejs, not browser

console.log(3 
    +
     3) // code readability should be high

console.log("Hitesh")


let name = "hitesh"
let age = 18
let isLoggedIn = false
let state;

// number => 2 to power 53
// bigint
// string => ""
// boolean => true/false
// null => standalone value
// undefined => 
// symbol => unique


// object

console.log(typeof undefined); // undefined
console.log(typeof null); // object